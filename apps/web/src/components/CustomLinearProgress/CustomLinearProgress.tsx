import {
  ProgressBarContainer,
  ProgressIndicator,
} from "./CustomLinearProgress.styles";

const CustomLinearProgress = () => {
  return (
    <ProgressBarContainer>
      <ProgressIndicator color="blue" />
    </ProgressBarContainer>
  );
};

export default CustomLinearProgress;
